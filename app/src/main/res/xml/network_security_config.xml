<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic to any host so ATTACK_SERVER can point to arbitrary IPs/ports -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
